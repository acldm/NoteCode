> 此部分包含JS五种实现oop继承的方法。

### 默认模式
直接将原型设置为父类的一个实例。

### 借用构造函数模式
将父类构造函数作为函数调用,并将上下文(this)设置为当前创建的新实例。

### 借用&设置构造函数模式模式
结合以上两种构造函数模式，即将又将子构造函数原型设置为父类实例，又将父类构造函数在子类构造函数中作为函数调用

### 共享模式
不直接调用父类构造函数,只将子类原型设置成父类原型，隐示将所有可复用部分都移到原型中

### 临时构造函数模式
与共享函数区别在于断开父类与子类原型对象之间的直接连接